# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# ⚠️  PRE-LAUNCH LOCKDOWN — DELETE THIS FILE AT DNS CUTOVER ⚠️
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# Blocks all crawlers from indexing the preview deployment
# (preview.buildodyssey.com + *.odyssey-site-305.pages.dev) so they don't
# get indexed as duplicates of the eventual apex site.
#
# When you cut DNS for buildodyssey.com → Cloudflare Pages:
#   1. Delete this file (public/robots.txt)
#   2. Remove the matching <meta name="robots"> tag from src/layouts/Layout.astro
#      (search for "PRE-LAUNCH LOCKDOWN")
#   3. npm run build && deploy
#   4. Submit buildodyssey.com to Google Search Console for re-indexing
#
# Added: 2026-05-26 per SEO consultant guidance.

User-agent: *
Disallow: /
