# Cloudflare Pages redirects.
# Format: <source> <destination> <status>

# Old slug → new slug for the estimate landing page. Any link reps already
# sent under /cost-summary keeps working.
/cost-summary       /estimate     301
/cost-summary/      /estimate     301

# Lots page renames. Originally 'Communities' (Umbraco-era) → 'Find a Lot'
# → 'Available Lots' (current). Preserves existing inbound links + Google
# rankings through both renames.
/communities        /available-lots   301
/communities/       /available-lots   301
/find-a-lot         /available-lots   301
/find-a-lot/        /available-lots   301
